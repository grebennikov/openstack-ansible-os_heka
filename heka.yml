- include: heka_install.yml
  vars: 
     heka_hosts: keystone_all
     heka_role: keystone

- include: heka_install.yml
  vars:
     heka_hosts: nova_all
     heka_role: nova

- include: heka_install.yml
  vars:
     heka_hosts: glance_all
     heka_role: glance

- include: heka_install.yml
  vars:
     heka_hosts: cinder_all
     heka_role: cinder

- include: heka_install.yml
  vars:
     heka_hosts: heat_all
     heka_role: heat

- include: heka_install.yml
  vars:
     heka_hosts: neutron_all
     heka_role: neutron

- include: heka_install.yml
  vars:
     heka_hosts: galera_all
     heka_role: mysql

- include: heka_install.yml
  vars:
     heka_hosts: rabbitmq_all
     heka_role: rabbitmq

- include: heka_install.yml
  vars:
     heka_hosts: horizon_all
     heka_role: horizon

- include: heka_install.yml
  vars:
     heka_hosts: compute_all
     heka_role: compute
